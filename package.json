{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "type": "module",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  },
  "include": [
    "src/**/*",
    "src/global.d.ts"
  ]
}
